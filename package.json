{
  "name": "opencode-background-ops",
  "version": "0.1.0",
  "type": "module",
  "description": "Single unified background operations for opencode: background tasks + bash",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.18.23"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "5.0.0",
    "typescript": "^5.6.0",
    "vitest": "5.0.0"
  }
}
